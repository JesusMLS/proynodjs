1. Se creo el archivo Dockerfile

2. Se creo el archivo docker-compose.yml

3. Se modifico app.js con las credenciales de mysql.

3. Se utilizaron los comandos:
-docker compose up -d
-docker ps
-docker images
-docker compose down
-docker compose down --rmi all

4. Se crearon las tablas dentro de 'prueba' con adminer:
USE `prueba`;
DROP TABLE IF EXISTS `users`;
CREATE TABLE `users` (
  `id` int NOT NULL AUTO_INCREMENT,
  `fname` varchar(255) DEFAULT NULL,
  `lname` varchar(255) DEFAULT NULL,
  PRIMARY KEY (`id`)
);